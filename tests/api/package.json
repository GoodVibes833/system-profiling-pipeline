{
  "name": "api-tests",
  "version": "1.0.0",
  "private": true,
  "description": "REST API tests using Jest + SuperTest",
  "scripts": {
    "test": "jest --runInBand --forceExit --detectOpenHandles"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
